{{>layout/header}}

    <h1>SpringBoot Web Service</h1>
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-6">
                <a href="/register" role="button" class="btn btn-primary">회원가입</a>
            </div>
            <br>
            <table class="table table-horizontal table-bordered">
                <thead class="thead-strong">
                <tr>
                    <th>token</th>
                    <th>loginiId</th>
                    <th>name</th>
                    <th>password</th>
                </tr>
                </thead>
                <tbody id="tbody">
                {{#members}}
                    <tr>
                        <td>{{token}}</td>
                        <td><a href="/register">{{LoginId}}</a></td>
                        <td>{{name}}</td>
                        <td>{{password}}</td>
                    </tr>
                {{/members}}
                </tbody>
            </table>
        </div>
    </div>

{{>layout/footer}}
